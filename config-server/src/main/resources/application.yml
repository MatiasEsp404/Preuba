server:
  port: ${PORT:8082}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MatiasEsp404/Preuba.git
          search-paths: config-data
          username: MatiasEsp404
          password: ${GIT_PASSWORD}